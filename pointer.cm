void foo(int* x) {}

int* ptr(int x) {
    return &x;
}

void add(int* x) {
    **x = *x + 1;
    int y;
    y = *x * *x;
    foo(&y);
    int a, *b;
    b = &y;
    // ptr(y) = b; this will be illegal (not lvalue)
}
