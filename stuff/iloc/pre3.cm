linked fn print<T>(_: T);

fn pre_example(x: int, y: int, z: int) {
    let a = 4;
    while (x < 5) {
        z = y + a;
        x += 1;
    }
    print(z);
}

fn main() {
    pre_example(3, 2, 0);
}
