linked fn print<T>(_: T);
linked fn scan<T>(_: *T);

fn main() {
    let z = 0;
    let x = 0;
    scan(&x);
    let y = 3;
    if (x > 3) {
        let k = x + x;
        z = x + y;
        print(k);
    } else {
        z = x + y;
        print("howdy");
    }

    print(z);
}
