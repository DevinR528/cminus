void foo(int* x) {}

int* ptr(int x) {
    return &x;
}

void add(int* x) {
    foo();
    **x = *x + 1; // cannot deref an int
    int y;
    y = *x * *x;
    foo(&y);
    int a, *b;
    b = &y;
    // ptr(y) = b; this will be illegal (not lvalue)
}
