linked fn printf<T>(_fmt: cstr, _: T);

fn main() {
    let x = false;
    // TODO: this is dumb make a better printf or something..
    printf("%s\n", x); // prints `false`
    printf("%s\n", true); // prints `true`
}
