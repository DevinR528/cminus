fn write(_: string) {}

fn main() {
    let x = 10;
    let y = 5;
    if (x > y) {
        write("bigger");
    } else {
        write("smaller");
    }

    if (x < y) {
        write("bigger");
    } else {
        write("smaller");
    }
}
