int set_idx(int idx, int y[3]) {
    return y[idx];
}

void main() {
    int a, b, c;
    int x[3];
    x = {2, 1, 3};
    a = set_idx(0, x);
    write(a);

    b = set_idx(1, x);
    write(b);

    c = set_idx(2, x);
    write(c);
}
