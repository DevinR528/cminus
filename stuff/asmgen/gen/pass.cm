linked fn write<T>(_: T);

fn pass<T>(a: T) {
    write::<T>(a);
}

fn main() {
    let x = 10;
    // let a = pass(x); // TODO: make this work using inference
    pass::<int>(x);

    pass::<string>("stringy");
}
