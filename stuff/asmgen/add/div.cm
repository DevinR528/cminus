fn write(x: int) {}

fn main() {
    let a = 100;
    let x = 10;
    let z = 5;
    let y = a / x / z;
    write(y);               // 2
    write(a / 2 / 2 / 5);   // 5
    write(30 / z);          // 6
    let b = a / y / y / 5;
    write(b);               // 5
}
