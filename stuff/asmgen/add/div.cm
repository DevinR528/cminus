void main() {
    int x, y, z, a, b;
    a = 100;
    x = 10;
    z = 5;
    y = a / x / z;
    write(y);               // 2
    write(a / 2 / 2 / 5);   // 5
    write(30 / z);          // 6
    b = a / y / y / 5;
    write(b);               // 5
}
