trait add<T> {
    T add(T a, T b);
}

impl add<int> {
    int add(int a, int b) {
        return a + b;
    }
}

// T foo<T: add>(T a, T b,) {
//     return <<T>::add>(a, b);
// }

void main() {
    int x, y, a;
    x = 10; y = 1;
    a = <<int>::add>(x, y);
    write(a);
}
