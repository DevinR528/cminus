fn add(int x, int y): int { return x + y; }

fn main() {
    let z = 4 >> 2 & 0 << 3;
    let x = 4 + 2 & 0 + 3;
    z += 1;
    // let y = z | (1 & ~5);
    z = y + add(1, 1);
    let a = 5;
    a += 5;
    let b = a + 1;
}
