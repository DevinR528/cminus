int add(int x, int y) { return x + y; }

void main() {
    int x, y, z, a, b;

    z = 4 >> 2 & 0 << 3;
    x = 4 + 2 & 0 + 3;
    z += 1;
    y = z | (1 & ~5);
    z = y + add(1, 1);
    a = 5;
    a += 5;
    b = a + 1;
}
