enum result<T, E> {
    ok(T),
    error(E),
}

void main() {
    enum result<int, char> res;
    res = result::ok(10);

    enum result<char, int> fib;
    fib = result::error('c'); // error on the correct generic param
}
