enum option<T> {
    some(T),
    none,
}

fn foo<T>(T it): option<T> {
    let opt = option::some(it);
    return opt;
}

fn main() {
    let num = foo::<option<int>>(foo::<int>(10));
    let ch = foo::<enum option<char>>(foo::<char>('c'));
}
