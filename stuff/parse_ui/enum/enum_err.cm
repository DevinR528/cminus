// Fail
enum option<T> {
    some(T),
    none,
}

void main() {
    enum option<int> num;
    num = option::some(10);

    match num {
        option::some(a) -> {
            a = "true"; // error: match wrong type bound from enum
        },
        option::none -> {
            exit;
        }
    }
}
