linked fn write(_: int);

fn main () {

  let a = 0; let b = 0; let c = 0;

  while (a < 50) {
      while (b < 10) {
        write (a+b);
        b = b + 2;
      }
      c = 0 - 10;
      while (c != b) {
        write(c);
        c = c + 1;
      }
      a = a + 10;
  }
    exit;
}
