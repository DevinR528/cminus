linked fn write<T>(_: T);
linked fn read<T>(_: T);

fn main() {
    let a = 'x';

    write::<string>("enter a:");
    read::<char>(a);
    if (a < 'n') {
        write::<char>('a');
    }
    else {
        write::<char>('z');
    }
}
