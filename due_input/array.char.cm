linked fn write<T>(_: T);

fn main() {
    let a = ['D'; 8];
    let i = 7;

    a[7] = 'a';
    write::<char>(a[3]);
    write::<char>(a[i]);

    a[i] = 'd';
    write::<char>(a[i]);
    write::<char>(a[7]);

    i -= 2;
    a[i] = 'x';
    write::<char>(a[5]);
}
