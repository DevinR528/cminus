linked fn write<T>(_: T);


fn main() {
    let c = [0; 10];
    let a = 0;
    while (a < 10) {
        c[a] = a;
        a += 1;
    }

	a = 0;
    while (a < 10) {
      if (a < 5) {
        write::<int>(c[a]);
      } else {
        write::<int>(0);
      }
	  a += 1;
	}

	exit;
}
