linked fn write<T>(_: T);
linked fn read<T>(_: T);

fn main() {
    let a = 0;

    write::<string>("enter a:");
    read::<int>(a);
    write::<int>(a);
    if (a < 0) {
        write::<int>(1);
    } else {
        write::<int>(0);
    }
}
