linked fn write<T>(_: T);
linked fn read(_: float);

fn main() {
    let a = 0.0;

    write("enter a:");
    read(a);
    write(a);
    if (a < 0.0) {
        write(1);
    } else {
        write(0);
    }
}
