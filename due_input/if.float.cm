linked fn write<T>(_: T);
linked fn read(_: float);

fn main() {
    let a = 0.0;

    write::<string>("enter a:");
    read(a);
    write::<int>(a);
    if (a < 0.0) {
        write::<int>(1);
    } else {
        write::<int>(0);
    }
}
